# Web 公务用车使用登记及管理系统 V1 实施 Issue 拆分

## 1. 拆分原则

- 每个 issue 只实现一个清晰功能边界。
- 每个 issue 实现前先写测试。
- 每个 issue 实现后必须运行验证。
- 不把新功能、bug 修复、重构混在同一个 issue。
- 不在当前 issue 中顺手实现后续 issue 的功能。
- 发现范围外问题时，记录为后续 issue，不混入当前改动。

## 2. Issue 总览

| Issue | 名称 | 类型 | 说明 |
| --- | --- | --- | --- |
| Issue 0 | 项目骨架、数据库迁移与测试基础设施 | 工程准备 | 不实现业务功能，只建立可运行、可测试、可初始化数据库的应用骨架 |
| Issue 1 | 登录、退出、修改本人密码 | 新功能 | 实现账号密码登录、退出登录、本人修改密码 |
| Issue 2 | 用户账号管理 | 新功能 | 管理员新增/删除用户、重置普通用户密码，保护内置 admin |
| Issue 3 | 公车档案管理 | 新功能 | 管理员新增/删除车辆，车辆下拉数据源 |
| Issue 4 | 公务用车登记与里程计算 | 新功能 | 登记表单、车辆独立里程、跨天、里程校验 |
| Issue 5 | 管理员记录管理与删除 | 新功能 | 管理员查看全部记录、删除记录、删除后里程回算 |
| Issue 6 | Excel 导出与权限收口 | 新功能 | 导出 Excel、导出排序、权限回归补强 |

## 3. Issue 0：项目骨架、数据库迁移与测试基础设施

### 3.1 目标

建立一个可本地运行、可写自动化测试、可持续迭代、可重复初始化数据库的 Web 应用骨架。不实现登录、车辆、记录、导出等业务功能。

### 3.1.1 技术路线

- 前端：React + Vite
- 后端：Node.js + Express
- 本地/测试数据库：SQLite
- 数据库创建方式：ORM migration 管表结构，seed 管初始数据
- 测试：单元/接口测试优先，后续 issue 再按功能补 UI 或 E2E 测试
- 运行方式：本地一条命令启动前端，一条命令启动后端，一条命令运行测试

### 3.2 范围内

- 创建前端与后端的最小可运行项目结构，或创建单体 Web 应用结构。
- 创建测试运行命令。
- 创建基础健康检查或占位页面。
- 创建基础目录约定，例如页面、接口、数据模型、测试目录。
- 明确本地启动命令和测试命令。
- 引入 SQLite 数据库连接。
- 引入 ORM migration 机制。
- 创建最小用户表结构，支撑后续 Issue 1 的 `admin/admin` 初始化。
- 创建 seed 脚本，写入初始管理员 `admin/admin`。
- 创建数据库重置或初始化命令，保证测试环境可重复准备。

### 3.3 范围外

- 不实现登录。
- 不实现用户、车辆、记录、导出。
- 不设计最终 UI。
- 不引入与当前系统无关的第三方能力。
- 不实现密码修改。
- 不实现用户管理接口。
- 不实现车辆表、用车记录表、导出逻辑。
- 不把 SQLite 切换到 PostgreSQL 或其他生产数据库。

### 3.4 测试优先要求

- 先写健康检查或首页加载测试。
- 先写测试命令可执行性验证。
- 验证应用能启动，测试命令能运行。
- 先写数据库 migration 测试，验证用户表能被创建。
- 先写 seed 测试，验证初始管理员 `admin/admin` 被写入。
- 先写重复执行 seed 的测试，验证不会创建重复 `admin` 账号。

### 3.5 验收标准

- 本地应用可启动。
- 基础页面或健康检查可访问。
- 测试命令可执行，且至少 1 条基础测试通过。
- README 或等价文档说明启动与测试命令。
- 执行数据库初始化命令后，SQLite 数据库文件可创建。
- 执行 migration 后，用户表存在且包含后续认证所需字段。
- 执行 seed 后，存在且仅存在 1 个内置管理员账号 `admin`。
- 重复执行 seed 不会生成重复 `admin` 账号。
- 数据库初始化、migration、seed 均可通过命令重复执行。

### 3.6 风险点

- 如果在 Issue 0 中实现业务功能，会污染后续 issue 边界。
- 如果测试基础设施不稳定，后续“先测后写”会难以执行。
- 如果数据库靠手工建表，后续测试环境无法稳定复现。
- 如果 seed 不是幂等的，重复初始化会制造重复账号并影响登录测试。
- 如果 Issue 0 提前创建车辆表或记录表，会和 Issue 3/Issue 4 混范围。

## 4. Issue 1：登录、退出、修改本人密码

### 4.1 目标

实现系统认证最小闭环：用户可账号密码登录、退出登录、修改本人密码。管理员与普通员工登录后能区分角色，为后续权限类 issue 提供基础。

### 4.2 范围内

- 登录页。
- 登录接口。
- 退出登录接口。
- 登录态保存与失效。
- 内置初始管理员 `admin/admin`。
- 用户角色字段：`admin` / `employee`。
- 修改本人密码入口、页面或弹窗。
- 修改本人密码接口。
- 修改密码成功后旧密码失效。
- 修改密码成功后当前会话失效，需重新登录。
- 登录失败统一提示“账号或密码错误”。

### 4.3 范围外

- 不实现用户管理页面。
- 不实现管理员新增/删除用户。
- 不实现车辆、登记、记录、导出。
- 不实现管理员重置他人密码。
- 不实现忘记密码。
- 不实现首次登录强制改密。
- 不实现密码复杂度策略，除非后续单独开 issue。

### 4.4 页面与接口

#### 页面

- 登录页：
  - 账号输入框
  - 密码输入框
  - 登录按钮
- 登录后占位页：
  - 展示当前登录用户账号
  - 展示当前角色
  - 管理员可见“查看全部”占位入口
  - 普通员工不可见“查看全部”入口
  - 修改密码入口
  - 退出登录入口
- 修改密码页或弹窗：
  - 当前密码
  - 新密码
  - 确认新密码
  - 提交修改
  - 取消

#### 接口

- `POST /api/login`
- `POST /api/logout`
- `POST /api/change-password`

### 4.5 数据规则

- 系统初始化存在 `admin/admin`。
- 用户账号全局唯一的完整管理能力放到 Issue 2，本 issue 只需满足认证所需的最小用户数据。
- 登录时账号不存在或密码错误，统一返回“账号或密码错误”。
- 未登录调用修改密码接口返回 `401` 或等价未登录响应。
- 修改密码时当前密码必须正确。
- 新密码与确认新密码必须一致。
- 新密码不能与当前密码相同。
- 修改密码只能作用于当前登录用户。

### 4.6 测试映射

本 issue 至少覆盖以下用例：

| 用例编号 | 覆盖点 |
| --- | --- |
| TC-LOGIN-001 | 管理员登录 |
| TC-LOGIN-002 | 普通员工登录 |
| TC-LOGIN-003 | 错误密码 |
| TC-LOGIN-004 | 不存在账号 |
| TC-LOGIN-005 | 账号为空 |
| TC-LOGIN-006 | 密码为空 |
| TC-PWD-001 | 普通员工修改本人密码 |
| TC-PWD-002 | 新密码可登录 |
| TC-PWD-003 | 旧密码失效 |
| TC-PWD-004 | 当前密码错误 |
| TC-PWD-005 | 两次新密码不一致 |
| TC-PWD-006 | 新旧密码相同 |
| TC-PWD-007 | 管理员修改本人密码 |
| TC-PWD-008 | 未登录调用修改密码接口 |
| TC-PWD-009 | 改密后当前会话失效 |
| TC-AUTH-001 | 未登录访问登记页 |

### 4.7 实现前测试要求

- 先写登录接口测试。
- 先写登录页表单校验测试。
- 先写角色入口可见性测试。
- 先写修改密码接口测试。
- 先写改密后旧密码失效与会话失效测试。

### 4.8 实现后验证要求

- 运行全部认证相关自动化测试。
- 手工验证管理员 `admin/admin` 首次登录。
- 手工验证普通员工登录入口差异。
- 手工验证修改密码后旧密码不可登录。
- 手工验证退出后访问受限页面需重新登录。

### 4.9 验收标准

- 正确账号密码可进入登录后页面。
- 管理员可见“查看全部”占位入口。
- 普通员工不可见“查看全部”入口。
- 错误账号或错误密码统一提示“账号或密码错误”。
- 账号或密码为空时不允许提交。
- 已登录用户可修改本人密码。
- 修改密码成功后旧密码失效，新密码可登录。
- 修改密码成功后当前会话失效。
- 未登录用户不能访问登录后页面。
- 未登录用户不能调用修改密码接口。

### 4.10 风险点

- 如果 Issue 1 中顺手实现用户管理，会和 Issue 2 混范围。
- 如果登录失败提示区分账号不存在和密码错误，会带来账号枚举风险。
- 如果改密后会话不失效，可能与验收标准不一致。
- 如果后续要引入密码加密、会话存储或数据库迁移，应在 Issue 1 内保持最小必要实现，不扩展到权限管理之外。

## 5. Issue 2 预拆分：用户账号管理

- 管理员查看用户列表。
- 管理员新增普通员工账号。
- 管理员新增管理员账号。
- 管理员重置普通用户密码。
- 管理员删除普通用户或非内置管理员。
- 初始 `admin` 不可删除。
- 普通员工不可访问用户管理页面和接口。
- 管理员重置普通用户密码不要求输入当前密码。
- 管理员不能重置内置 `admin` 或其他管理员密码。
- 删除用户取消确认时数据不变。
- 删除用户确认后该用户不可重新登录。

## 6. Issue 3 预拆分：公车档案管理

- 管理员查看车辆列表。
- 管理员新增车辆。
- 车辆编号唯一。
- 车牌号码唯一。
- 管理员删除车辆。
- 删除车辆取消确认时数据不变。
- 删除车辆后从登记页下拉框移除。
- 删除车辆不删除历史记录。

## 7. Issue 4 预拆分：公务用车登记与里程计算

- 登记页完整表单。
- 车辆下拉框。
- 每辆车独立默认起步公里。
- 首条记录起步公里留空手填。
- 终点公里自动计算行车公里。
- 支持跨天。
- 里程边界与格式校验。
- 提交成功后保留当前车辆并刷新起步公里。

## 8. Issue 5 预拆分：管理员记录管理与删除

- 管理员查看全部有效记录。
- 普通员工不可查看全部记录。
- 管理员删除记录。
- 删除记录取消确认时数据不变。
- 删除记录后列表与导出排除该记录。
- 删除最新记录后，车辆默认起步公里按剩余有效记录重算。
- V1 不提供记录编辑入口。

## 9. Issue 6 预拆分：Excel 导出与权限收口

- 管理员导出全部有效记录为 `.xlsx`。
- 导出表头与列表一致。
- 导出排序按车辆编号升序，同车内按业务日期升序、创建时间升序。
- 已删除车辆历史记录仍导出。
- 已删除记录不导出。
- 无记录时导出只有表头的空文件。
- 普通员工调用导出接口返回 `403` 或等价无权限响应。
