# Web 公务用车使用登记及管理系统 优化需求 Spec

## 1. 文档信息

- 文档版本：V1.1
- 文档日期：2026-07-20
- 文档状态：可用于优化设计、开发、测试评审
- 适用范围：基于当前已上线 V1 功能的交互优化与规则补充

## 2. 优化目标

在不改变系统核心业务范围的前提下，优化管理员与普通用户的高频操作体验，减少页面滚动成本、提升记录定位效率、降低误操作风险，并补充少量权限规则。

## 3. 范围说明

### 3.1 本次范围内

- 用车记录排序优化
- 各模块展开区交互优化
- 时间选择交互优化
- 记录搜索与筛选
- 表单与删除交互优化
- 管理区结构优化
- 成功反馈与空状态优化
- 管理员权限规则补充

### 3.2 本次不包含

- 新增审批流、派车流、编辑记录
- 新增导出筛选结果逻辑
- 新增用户列表和车辆列表搜索
- 调整默认起步公里业务规则
- 单点登录、消息通知、审计日志

## 4. 详细优化项

### 4.1 用车记录倒序展示

- 用车记录管理区域按记录创建时间倒序展示。
- 最新创建的记录排在最上方。

### 4.2 操作区就地下展开

- 点击 `修改密码`、`重置密码`、`新增用户`、`新增车辆`、`删除`、`删除记录` 后，在当前模块下方直接展开对应操作区。
- 不再将填写区或确认区统一放置到页面最底部。

### 4.3 展开区支持再次收回

- `查看全部`、`修改密码`、`重置密码`、`新增用户`、`新增车辆`、`删除`、`删除记录` 等按钮支持二次点击收回。
- 同一按钮第一次点击为展开，第二次点击为收回。

### 4.4 时间选择补全到月日时分

- `出车时间`、`还车时间` 选择项需支持选择月、日、时、分。
- 用户在填写时应能直接识别时间先后关系和是否跨天。

### 4.5 表单错误就地提示

- 登记、新增、修改密码、重置密码等表单发生校验失败时，在对应字段附近显示错误提示。
- 提交失败后，页面应聚焦到第一个错误项或让用户明显感知首个错误位置。

### 4.6 删除对象更易识别

- 删除用户、删除车辆、删除记录时，按钮文案或确认区需带上足够的对象标识。
- 删除记录至少应体现记录关键信息，避免仅依赖简短事由造成误删。

### 4.7 管理区层次更清晰

- `查看全部` 展开后，用户管理、车辆管理、记录管理区域需具备更清晰的结构层次。
- 应让管理员能快速区分自己当前正在操作的模块。

### 4.8 记录支持搜索与筛选

- 用车记录管理区域增加搜索能力。
- 支持按车辆、登记人、日期筛选记录。
- 搜索与筛选结果仅影响记录展示，不改变底层原始数据。

### 4.9 成功反馈更完整

- 提交登记、新增、删除、重置密码、导出等操作成功后，页面应给出明确成功反馈。
- 不同模块的成功提示风格应尽量统一。

### 4.10 空状态更明确

- 当用户列表、车辆列表、记录列表为空时，应显示清晰空状态。
- 用户应能区分“当前无数据”和“页面异常/未加载成功”。

### 4.11 内置 admin 权限放宽

- 用户名为 `admin` 的内置账号，可重置其他所有用户的密码。
- “其他所有用户”包含普通员工和非内置管理员。
- `admin` 修改自己密码仍走“修改本人密码”流程，不走重置他人密码流程。

### 4.12 非 admin 管理员不可删除自己

- 非 `admin` 的管理员账号，不允许删除自己的账号。
- 用户删除接口和页面操作均需遵守该限制。

## 5. 关键交互规则

- 模块内展开区优先就地展示，不跨模块跳转到底部。
- 同一时刻可只允许一个同类操作区展开，避免页面内出现多个重叠待操作区。
- 搜索和筛选清空后，记录列表恢复默认倒序全量展示。
- 记录默认展示顺序与搜索/筛选前的基础排序保持一致。

## 6. 风险点

- 若展开区状态管理不清晰，可能出现多个模块同时展开，影响阅读和操作。
- 若倒序展示与原导出排序混用，容易让用户误解页面顺序与导出顺序必须一致。
- 若时间控件改造不完整，可能影响现有跨天判定与表单提交逻辑。
- 若权限规则只改前端未改后端，仍存在绕过页面直接调用接口的风险。
- 若记录搜索筛选仅做界面处理但缺少清晰反馈，用户可能误判为数据丢失。

## 7. 建议实施优先级

### P1

- 用车记录倒序展示
- 操作区就地下展开
- 展开区支持再次收回
- 时间选择补全到月日时分
- 内置 `admin` 权限放宽
- 非 `admin` 管理员不可删除自己

### P2

- 表单错误就地提示
- 删除对象更易识别
- 管理区层次更清晰
- 记录支持搜索与筛选

### P3

- 成功反馈更完整
- 空状态更明确
