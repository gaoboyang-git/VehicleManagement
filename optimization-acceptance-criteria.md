# Web 公务用车使用登记及管理系统 优化项验收标准

## 1. 文档信息

- 版本：V1.1
- 日期：2026-07-20
- 适用对象：产品、研发、测试、验收方
- 依据文档：`optimization-spec.md`

## 2. 总体验收原则

- 本文档仅验收优化项，不替代既有 `V1` 基线功能验收。
- 所有优化项应在不破坏现有登录、登记、管理、导出主流程的前提下交付。
- 页面交互优化需同时验证前端表现与后端规则，不允许仅界面生效、接口规则缺失。
- 本轮验收以“操作更顺手、反馈更明确、权限更准确、定位更高效”为通过标准。

## 3. 模块验收标准

### 3.1 用车记录倒序展示

1. 管理员打开用车记录管理区域时，记录默认按创建时间倒序展示。
2. 最新创建的记录应排在最上方。
3. 删除一条记录后，剩余记录仍保持创建时间倒序。
4. 搜索或筛选清空后，记录列表恢复默认倒序展示。

### 3.2 操作区就地下展开

1. 点击 `修改密码` 后，修改密码区域应在当前功能位置附近展开，不应跳转到页面最底部。
2. 点击 `新增用户` 后，新增用户填写区应在用户管理模块下方展开。
3. 点击 `重置密码` 后，重置密码填写区应在对应用户管理模块下方展开。
4. 点击 `新增车辆` 后，新增车辆填写区应在车辆管理模块下方展开。
5. 点击 `删除` 用户或车辆后，待确认删除区应在当前对应模块下方展开。
6. 点击 `删除记录` 后，待确认删除区应在记录管理模块下方展开。

### 3.3 展开区支持再次收回

1. `查看全部` 点击一次展开管理区，再点击一次收回管理区。
2. `修改密码` 点击一次展开修改密码区，再点击一次收回修改密码区。
3. `新增用户`、`重置密码`、`新增车辆`、`删除`、`删除记录` 均支持再次点击收回当前展开区。
4. 收回后，页面不应保留对应展开区内容。
5. 同一类操作区重复切换时，页面不应出现多个重复展开区。

### 3.4 时间选择补全到月日时分

1. `出车时间` 支持选择月、日、时、分。
2. `还车时间` 支持选择月、日、时、分。
3. 用户在界面上可直接区分出车与还车的完整时间先后关系。
4. 选择完整时间后，记录提交逻辑仍可正常保存。
5. 跨天场景下，完整时间选择不应破坏现有跨天判断逻辑。

### 3.5 表单错误就地提示

1. 登记表单缺少必填项时，错误提示应显示在对应字段附近。
2. 新增用户表单缺少必填项时，错误提示应显示在对应字段附近。
3. 新增车辆表单缺少必填项时，错误提示应显示在对应字段附近。
4. 修改密码与重置密码表单校验失败时，错误提示应显示在对应字段附近。
5. 提交失败后，用户能明确知道优先修正哪一项。

### 3.6 删除对象更易识别

1. 删除用户时，确认区应明确展示目标用户名。
2. 删除车辆时，确认区应明确展示车辆编号或车牌号等关键信息。
3. 删除记录时，确认区应明确展示足以识别该记录的关键信息。
4. 当存在多条相似记录时，用户仍能区分当前待删除对象。

### 3.7 管理区层次更清晰

1. 点击 `查看全部` 后，用户管理、车辆管理、记录管理区域应具备明确分区。
2. 管理员可在一次页面浏览中快速识别当前所在模块。
3. 各模块的操作入口、列表区、展开区之间应有明显层次区分。

### 3.8 记录支持搜索与筛选

1. 用车记录管理区域提供搜索入口。
2. 支持按车辆筛选记录。
3. 支持按登记人筛选记录。
4. 支持按日期筛选记录。
5. 搜索条件生效后，记录列表应只展示符合条件的数据。
6. 多条件组合时，结果应满足全部已选条件。
7. 清空搜索与筛选后，记录列表恢复默认全量倒序展示。
8. 搜索与筛选仅影响显示结果，不改变原始记录数据。

### 3.9 成功反馈更完整

1. 提交登记成功后，页面应展示明确成功反馈。
2. 新增用户、新增车辆成功后，页面应展示明确成功反馈。
3. 删除用户、删除车辆、删除记录成功后，页面应展示明确成功反馈。
4. 重置密码、修改密码、导出 Excel 成功后，页面应展示明确成功反馈。
5. 不同模块的成功反馈样式和语义应保持基本一致。

### 3.10 空状态更明确

1. 用户列表为空时，应展示明确空状态。
2. 车辆列表为空时，应展示明确空状态。
3. 记录列表为空时，应展示明确空状态。
4. 空状态文案应让用户能区分“当前无数据”与“加载失败”。

### 3.11 内置 admin 权限放宽

1. 用户名为 `admin` 的内置账号可重置普通员工密码。
2. 用户名为 `admin` 的内置账号可重置非内置管理员密码。
3. `admin` 通过重置密码功能修改他人密码时，不要求输入对方当前密码。
4. 重置成功后，被重置用户旧密码立即失效，新密码可登录。
5. `admin` 修改自己密码仍应通过“修改本人密码”入口，不应通过重置他人密码入口绕行。

### 3.12 非 admin 管理员不可删除自己

1. 非 `admin` 的管理员账号登录后，不应允许删除自己的账号。
2. 页面上若存在删除入口，应在交互上拦截该操作，或不展示删除入口。
3. 即使绕过页面直接调用删除接口，后端也应拒绝删除自己的管理员账号。
4. 拒绝自删时，不得产生实际数据删除。

## 4. 交付通过条件

- 优化项不破坏原有主流程：
  - 登录
  - 修改本人密码
  - 新增用户
  - 新增车辆
  - 提交用车登记
  - 删除记录
  - 导出 Excel
- 关键优化项全部通过：
  - 用车记录倒序展示
  - 操作区就地下展开
  - 展开区支持再次收回
  - 时间选择补全到月日时分
  - 记录搜索与筛选
  - `admin` 可重置其他所有用户密码
  - 非 `admin` 管理员不可删除自己
- 权限相关优化前后端规则一致，无越权、漏拦截问题。
