# Web 公务用车使用登记及管理系统 V1 验收标准

## 1. 文档信息

- 版本：V1.0
- 日期：2026-07-20
- 适用对象：产品、研发、测试、验收方
- 依据文档：`spec.md`

## 2. 总体验收原则

- 所有页面、接口、权限和数据规则应与 `spec.md` 保持一致。
- 验收以“功能可用、规则正确、权限清晰、异常可控、数据可追溯”为准。
- V1 仅验收范围内功能，不以暂不做范围作为缺陷。
- 未登录访问页面时应跳转登录页；未登录访问接口时返回 `401` 或等价未登录响应。
- 已登录但角色无权限访问接口时返回 `403` 或等价无权限响应，且不得产生数据变更。
- 所有删除类操作在点击取消时不得删除数据；点击确认后才允许执行删除。
- 列表与导出的排序默认按车辆编号升序，同一车辆内按业务日期升序、创建时间升序排列。
- Excel 导出无有效记录时，默认下载仅包含表头、无数据行的 `.xlsx` 文件。

## 3. 模块验收标准

### 3.1 登录与退出

1. 用户输入正确账号密码时可成功登录系统。
2. 用户输入错误账号或错误密码时，系统统一提示“账号或密码错误”。
3. 未填写账号或密码时，不允许提交登录。
4. 普通员工登录后只能看到登记页，无管理入口。
5. 管理员登录后可看到登记页及“查看全部”管理入口。
6. 用户退出后，原登录态失效，再访问受限页面需重新登录。
7. 被管理员删除的用户不可再使用原账号密码登录。

### 3.2 修改密码

1. 已登录用户可打开修改密码页面或弹窗。
2. 当前密码正确且两次输入的新密码一致时，可修改成功。
3. 新密码不能与当前密码相同。
4. 当前密码错误时，不允许修改成功。
5. 确认新密码与新密码不一致时，不允许修改成功。
6. 修改成功后旧密码立即失效，新密码可用于重新登录。
7. 普通员工只能修改本人密码。
8. 管理员通过本人修改密码入口也只能修改本人密码；重置普通用户密码需走用户管理功能。
9. 未登录调用修改密码接口时，不允许修改密码。
10. 修改密码成功后，当前会话失效，用户访问登记页或管理页时需重新登录。

### 3.3 管理员重置普通用户密码

1. 管理员可在用户管理中打开普通用户密码重置入口。
2. 重置普通用户密码时，不要求输入该用户当前密码。
3. 重置普通用户密码时，新密码与确认新密码必须一致。
4. 重置成功后，该普通用户旧密码立即失效。
5. 重置成功后，该普通用户可使用新密码重新登录。
6. 管理员不能重置内置 `admin` 账号密码。
7. 管理员不能重置其他管理员账号密码。
8. 未登录用户不能调用重置普通用户密码接口。
9. 普通员工不能访问重置普通用户密码功能及接口。

### 3.4 用户账号管理

1. 管理员可查看全部用户列表。
2. 管理员可新增普通员工账号和管理员账号。
3. 新增用户时，账号、密码、角色均为必填。
4. 重复账号不可创建，系统应明确提示。
5. 管理员可删除普通用户和非内置管理员账号。
6. 删除用户前必须二次确认。
7. 初始 `admin` 账号不可删除。
8. 普通员工无法访问用户管理功能及接口。
9. 删除用户弹窗点击取消后，用户列表不变，该用户仍可使用原账号密码登录。
10. 删除用户弹窗点击确认后，用户列表不再展示该用户，该用户不可再登录。

### 3.5 公车档案管理

1. 管理员可查看全部车辆列表。
2. 管理员可新增车辆。
3. 新增车辆时，车辆编号、车牌号码、品牌型号均为必填。
4. 重复车辆编号不可创建。
5. 重复车牌号码不可创建。
6. 管理员可删除车辆。
7. 删除车辆前必须二次确认。
8. 删除车辆后，该车不再出现在登记页车辆下拉框中。
9. 删除车辆不影响历史用车记录保存与导出。
10. 普通员工无法访问车辆管理功能及接口。
11. 删除车辆弹窗点击取消后，车辆列表与登记页下拉框均不变化。

### 3.6 公务用车登记

1. 已登录用户可进入登记页并看到车辆下拉框。
2. 车辆下拉展示格式为“车辆编号 + 车牌号”。
3. 选择车辆后，系统按该车辆最近一条有效记录自动回填默认起步公里。
4. 若车辆无历史记录，起步公里应为空，由用户手动填写。
5. 切换车辆后，表单已填写内容会清空。
6. 日期默认当天，可手动修改。
7. 出车时间、还车时间、事由、路线、起步公里、终点公里、驾驶员签字为必填项。
8. 终点公里录入后，系统自动计算行车公里数。
9. 行车公里数必须等于终点公里减起步公里。
10. 终点公里小于起步公里时，不允许提交成功。
11. 允许跨天录入；当还车时间早于出车时间时，系统按次日还车处理。
12. 提交成功后，表单清空但保留当前选中车辆。
13. 提交成功后，当前车辆默认起步公里更新为本次终点公里。
14. 终点公里等于起步公里时，行车公里数为 `0`，允许提交。
15. 起步公里被用户手动修改后，行车公里数应基于用户当前输入值计算。
16. 起步公里或终点公里为负数、非数字或空格字符串时，不允许提交。
17. 当车辆已被删除但用户仍停留在旧登记页提交时，不允许保存记录，并提示车辆不存在或已失效。

### 3.7 记录管理

1. 管理员可查看全部有效用车记录。
2. 记录表格字段应包含车辆信息、登记人账号、日期、出车/还车时间、里程、加油信息、驾驶员、备注等核心数据。
3. 普通员工不可查看全量记录。
4. 管理员可删除任意记录。
5. 删除记录前必须二次确认。
6. 已删除记录不应继续出现在记录列表中。
7. V1 不应存在记录编辑入口。
8. 删除记录后，该车辆后续默认起步公里应基于剩余有效记录重新计算。
9. 删除记录弹窗点击取消后，记录列表不变，该记录仍出现在导出结果中。

### 3.8 Excel 导出

1. 管理员可导出全部有效记录为 Excel。
2. 导出文件应由浏览器自动下载。
3. Excel 表头应与记录列表字段一致。
4. 导出内容应按车辆编号升序排列，同一车辆内按业务日期升序、创建时间升序排列。
5. 已删除车辆对应的历史记录仍需出现在导出文件中。
6. 已删除记录不应出现在导出文件中。
7. 无记录时导出 `.xlsx` 文件，文件包含表头且无数据行。
8. Excel 数据行数应等于管理页全部有效记录数。
9. Excel 文件应可被 Excel 或 WPS 打开，且车辆编号、车牌、日期、里程字段可读。

### 3.9 权限与安全

1. 未登录用户访问受限页面时，应被拦截至登录页或收到未授权响应。
2. 普通员工访问管理员接口时，应返回 `403` 或等价无权限响应，且不得产生数据变更。
3. 登录失败提示不得暴露“账号不存在”或“密码错误”的具体区别。
4. 修改密码接口不得允许越权修改他人密码。

## 4. 数据验收标准

1. 用户账号在系统内全局唯一。
2. 车辆编号在系统内全局唯一。
3. 车牌号码在系统内全局唯一。
4. 每辆车的默认起步公里只受该车辆自身有效记录影响。
5. 删除车辆不会删除历史记录。
6. 删除记录后，默认起步公里以剩余有效记录为准。
7. 修改密码成功后，旧密码失效，新密码生效。

## 5. 交付通过条件

- P0 功能全部通过验收。
- P1 功能无阻塞性缺陷。
- 关键主流程均可独立完成：
  - 登录
  - 修改密码
  - 新增车辆
  - 新增用户
  - 提交用车登记
  - 删除记录
  - 导出 Excel
- 权限校验无越权问题。
- 导出结果与页面数据保持一致。
