# 产品需求文档（Web公务用车使用登记及管理系统）

## 1. 产品概述

### 1.1 产品简介

本产品为企业Web端公务用车登记管理系统，面向企业内部人员完成公车出车登记、里程自动计算、用车记录统一管理、公车基础档案维护、用户账号权限管控、记录Excel导出等核心能力。系统区分普通员工与管理员两类账号，基于账号密码登录，实现一车一档独立里程数据隔离，自动化里程计算，支持管理员统一维护车辆、管理账号、全量用车数据查询与导出，规范企业公务用车台账管理。

### 1.2 目标用户

1. **普通账户（员工）**：企业内部用车人员，仅可选择公车、填写用车登记表单提交记录，无后台管理、数据导出、车辆/账号维护权限。
2. **管理员账户（行政/后勤）**：负责系统账号新增删除、公车档案新增删除、全量用车记录查看、用车记录Excel批量导出，拥有全部操作权限；系统预置初始管理员账号：账号admin，密码admin。

## 2. 功能需求

### 2.1 用户登录功能

#### 2.1.1 账号密码登录

1. 系统仅支持账号密码登录方式，无短信、验证码、第三方登录渠道。
2. 登录页面包含输入项：账号输入框、密码输入框、登录按钮；表单提交时校验账号、密码非空。
3. 账号校验规则：账号区分管理员/普通用户，匹配对应密码后完成登录跳转；账号不存在、密码错误均统一返回登录失败提示。
4. 系统初始化内置管理员账号：账号`admin`，密码`admin`，不可直接删除该初始管理员账号。

#### 2.1.2 用户账号管理（管理员专属）

仅管理员登录后可见账号管理操作，包含新增用户、删除用户功能：

1. **新增用户**：弹窗录入账号、密码、用户类型（普通账户/管理员）；账号全局唯一，重复则提示报错。
2. **删除用户**：账号列表展示全部系统用户，单条记录配备删除按钮；删除前弹出二次确认弹窗；初始admin账号禁止删除。

### 2.2 公车档案管理（管理员专属）

#### 2.2.1 公车新增功能

1. 录入字段：车辆编号、车牌号码、车辆品牌型号，三项全部为必填项。
2. 唯一性校验：车辆编号、车牌号码全局不可重复，录入重复值提交时拦截并提示。
3. 提交后车辆自动存入公车下拉数据源，供登记页面调用。

#### 2.2.2 公车删除功能

公车列表展示全部已录入车辆，每条车辆数据配备删除按钮，点击弹出确认弹窗；删除后该车从登记页面下拉选项中移除，历史用车记录保留不删除。

### 2.3 公务用车登记表单功能

#### 2.3.1 公车选择下拉框

1. 表单页面最顶部放置公车下拉选择框，下拉数据源为管理员维护的全部公车信息（展示格式：车辆编号+车牌）。
2. 切换选择不同公车时，表单所有填写内容清空，**里程数据独立加载**：读取当前选中车辆上一条用车记录的终点公里数，自动回填至「起步公里读数」输入框。

#### 2.3.2 登记表单输入项（自上而下顺序）

表单依次展示以下输入框：

1. 日期：日期选择框，默认填充当日日期
2. 出车时间：时间选择框
3. 还车时间：时间选择框
4. 事由：单行文本输入框，填写公务出行事由
5. 目的地及行车路线：多行文本输入框
6. 起步公里读数：数字输入框，自动读取所选车辆上一条记录终点里程，支持手动修改
7. 终点公里读数：数字输入框，填写完成后自动计算行车公里数
8. 行车公里数：只读展示框，计算公式：行车公里数 = 终点公里读数 - 起步公里读数
9. 加油费用/数量：文本输入框，填写加油金额或油量
10. 驾驶员签字：文本输入框，填写驾驶员姓名
11. 备注：多行文本输入框，补充额外用车说明

#### 2.3.3 管理员专属入口按钮

管理员登录后，登记表单页面顶部展示「查看全部」按钮；普通用户无此按钮。点击按钮跳转至管理员管理页面。

### 2.4 管理员记录管理页面功能

#### 2.4.1 全量用车记录表格展示

页面以表格形式展示系统所有公车全部用车登记记录，表格字段与登记表单字段一一对应，包含车辆编号、车牌、登记人账号、日期、出车/还车时间、里程、加油信息、驾驶员等完整数据。

#### 2.4.2 Excel导出功能

页面固定放置「导出全部为excel表格」按钮，点击后系统将表格内全部用车记录生成Excel文件，浏览器自动下载；Excel表头与表格展示字段保持一致，按车辆分类有序展示数据。

## 3. 完整交互流程

### 3.1 用户登录流程

1. 用户访问Web系统首页，进入登录页面；
2. 用户输入账号、密码，点击登录按钮提交表单；
3. 系统校验账号密码：
   - 校验失败：页面提示账号或密码错误，停留在登录页；
   - 校验成功：根据账号类型跳转页面
     - 普通账户：直接跳转公务用车登记表单页面；
     - 管理员账户：跳转公务用车登记表单页面，页面显示「查看全部」管理入口按钮。

### 3.2 用车登记表单填写交互流程

1. 用户进入登记页面，顶部公车下拉框默认选中第一条公车数据；
2. 系统自动读取当前选中车辆上一条用车记录终点里程，回填至起步公里读数；
3. 用户填写日期、出车时间、事由、路线等基础信息；
4. 用户录入终点公里读数，输入框失去焦点后自动计算并展示行车公里数；
5. 补充加油信息、驾驶员签字、备注后，点击提交按钮保存本条用车记录；
6. 提交成功后表单清空，下拉框保持当前选中车辆，起步公里读数自动更新为本次终点里程；
7. 用户切换下拉框其他公车，表单清空，重新加载对应车辆历史终点里程作为起步里程。

### 3.3 管理员后台页面跳转与导出流程

1. 管理员在登记页面点击「查看全部」按钮，跳转管理员综合管理页面；
2. 管理页面分为两大模块：公车档案管理、用户账号管理、全量用车记录表格；
3. 管理员可执行新增/删除公车、新增/删除系统用户操作；
4. 管理员查看全部用车记录表格，点击「导出全部为excel表格」按钮；
5. 后端生成Excel文件，前端触发浏览器下载，完成导出；
6. 管理员可返回登记表单页面继续填写用车记录。

### 3.4 公车数据独立隔离交互流程

1. 管理员新增多条公车档案，每条车辆独立存储里程台账；
2. 用户选择车辆A登记，保存终点里程1000；再次选择车辆A，起步公里默认1000；
3. 用户切换至车辆B，起步公里读取车辆B上一条记录里程，不受车辆A数据影响；
4. 删除公车仅移除下拉选项，该车历史所有用车记录永久留存，导出Excel可正常查询。

### 3.5 管理员账号新增/删除流程

1. 管理员进入管理页面，打开用户账号管理模块；
2. 点击新增用户，填写账号、密码、用户类型，提交时校验账号唯一性；
3. 账号列表展示所有用户，单条点击删除按钮，弹窗确认后删除普通用户；
4. 针对初始admin账号，删除按钮置灰不可点击，系统拦截删除操作。
