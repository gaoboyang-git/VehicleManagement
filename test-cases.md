# Web 公务用车使用登记及管理系统 V1 测试用例

## 1. 文档信息

- 版本：V1.0
- 日期：2026-07-20
- 依据文档：`spec.md`
- 用例粒度：执行级
- 优先级说明：
  - `P0`：核心主流程或高风险权限场景
  - `P1`：重要功能与关键异常场景
  - `P2`：一般异常与边界场景

## 2. 测试前置说明

- 已部署可访问的测试环境。
- 系统已预置初始管理员 `admin/admin`。
- 准备至少 1 个普通员工账号和 1 个非内置管理员账号。
- 测试环境支持浏览器文件下载。

## 3. 测试用例

| 用例编号 | 优先级 | 用例标题 | 前置条件 | 测试步骤 | 预期结果 |
| --- | --- | --- | --- | --- | --- |
| TC-LOGIN-001 | P0 | 管理员使用正确账号密码登录 | 存在账号 `admin/admin` | 1. 打开登录页 2. 输入 `admin` 和正确密码 3. 点击登录 | 页面进入登记页，可见“查看全部”入口 |
| TC-LOGIN-002 | P0 | 普通员工使用正确账号密码登录 | 已存在普通员工账号 | 1. 打开登录页 2. 输入普通员工账号密码 3. 点击登录 | 页面进入登记页，不显示“查看全部”入口 |
| TC-LOGIN-003 | P0 | 登录时输入错误密码 | 已存在有效账号 | 1. 输入正确账号 2. 输入错误密码 3. 点击登录 | 登录页不跳转，提示“账号或密码错误” |
| TC-LOGIN-004 | P1 | 登录时输入不存在账号 | 无 | 1. 输入不存在账号 2. 输入任意密码 3. 点击登录 | 登录页不跳转，提示“账号或密码错误”，页面不展示账号是否存在 |
| TC-LOGIN-005 | P1 | 登录时账号为空 | 无 | 1. 账号留空 2. 输入任意密码 3. 点击登录 | 登录请求未发起或被拒绝，账号输入框展示必填提示 |
| TC-LOGIN-006 | P1 | 登录时密码为空 | 无 | 1. 输入任意账号 2. 密码留空 3. 点击登录 | 登录请求未发起或被拒绝，密码输入框展示必填提示 |
| TC-PWD-001 | P0 | 普通员工成功修改本人密码 | 普通员工已登录 | 1. 打开修改密码弹窗 2. 输入正确当前密码 3. 输入新密码和确认新密码且一致 4. 提交 | 页面提示密码已修改，当前登录态失效，需要重新登录 |
| TC-PWD-002 | P0 | 修改密码后使用新密码重新登录 | 已准备账号 `pwd_user`，旧密码为 `OldPass001`，新密码已变更为 `NewPass001` | 1. 打开登录页 2. 输入 `pwd_user` 和 `NewPass001` 3. 点击登录 | 页面进入登记页，登录用户显示为 `pwd_user` 或等价当前用户信息 |
| TC-PWD-003 | P0 | 修改密码后旧密码失效 | 已准备账号 `pwd_user`，旧密码为 `OldPass001`，新密码已变更为 `NewPass001` | 1. 打开登录页 2. 输入 `pwd_user` 和 `OldPass001` 3. 点击登录 | 登录页不跳转，提示“账号或密码错误” |
| TC-PWD-004 | P1 | 当前密码错误时修改密码 | 已登录 | 1. 打开修改密码弹窗 2. 输入错误当前密码 3. 输入一致的新密码 4. 提交 | 页面提示当前密码错误；使用原密码仍可登录 |
| TC-PWD-005 | P1 | 两次新密码不一致时修改密码 | 已登录 | 1. 输入正确当前密码 2. 输入不一致的新密码和确认新密码 3. 提交 | 页面提示两次新密码不一致；当前登录态保持不变 |
| TC-PWD-006 | P1 | 新密码与当前密码相同 | 已登录 | 1. 输入正确当前密码 2. 新密码填写与当前密码相同 3. 提交 | 页面提示新密码不能与当前密码相同；使用原密码仍可登录 |
| TC-PWD-007 | P0 | 管理员成功修改本人密码 | 非内置管理员 `mgr_pwd` 已登录，当前密码为 `OldAdmin001` | 1. 打开修改密码弹窗 2. 输入 `OldAdmin001` 3. 新密码和确认新密码输入 `NewAdmin001` 4. 提交 | 页面提示密码已修改，当前登录态失效；使用 `NewAdmin001` 可重新登录，使用 `OldAdmin001` 登录页不跳转 |
| TC-PWD-008 | P0 | 未登录调用修改密码接口 | 当前浏览器无有效登录态 | 1. 调用 `POST /api/change-password` 2. 请求体传入任意当前密码和新密码 | 接口返回未登录响应，账号密码未发生变化 |
| TC-PWD-009 | P0 | 修改密码成功后当前会话失效 | 普通员工已登录并完成密码修改提交 | 1. 修改密码成功后直接访问登记页 | 页面跳转登录页或返回未登录响应，需要使用新密码重新登录 |
| TC-USER-001 | P0 | 管理员新增普通员工账号成功 | 管理员已登录并进入管理页 | 1. 打开新增用户弹窗 2. 填写唯一账号、密码、角色为普通员工 3. 提交 | 用户弹窗关闭，用户列表新增 1 行，账号和角色与输入一致 |
| TC-USER-002 | P0 | 管理员新增管理员账号成功 | 管理员已登录并进入管理页 | 1. 打开新增用户弹窗 2. 填写唯一账号、密码、角色为管理员 3. 提交 | 用户弹窗关闭，用户列表新增 1 行，账号和角色与输入一致 |
| TC-USER-003 | P1 | 新增用户时账号重复 | 已存在账号 `user001` | 1. 打开新增用户弹窗 2. 填写账号 `user001` 及其他必填项 3. 提交 | 用户列表行数不变，页面提示账号重复 |
| TC-USER-004 | P1 | 新增用户时缺少必填字段 | 管理员已登录 | 1. 打开新增用户弹窗 2. 分别缺失账号、密码、角色后提交 | 用户列表行数不变，缺失字段展示必填提示 |
| TC-USER-005 | P0 | 管理员删除普通用户成功 | 已存在可删除普通用户 | 1. 在用户列表点击删除 2. 确认删除 | 用户列表删除该账号行，该账号无法使用原密码登录 |
| TC-USER-006 | P1 | 管理员删除初始 admin 账号 | 管理员已登录 | 1. 在 `admin` 账号行尝试点击删除 | 删除按钮不可用或删除被系统拦截，`admin` 账号仍存在 |
| TC-USER-007 | P0 | 普通员工访问用户管理页 | 普通员工已登录 | 1. 直接访问用户管理页面或接口 | 页面不可访问或接口返回无权限 |
| TC-USER-008 | P1 | 删除用户弹窗点击取消 | 管理员已登录，存在账号 `cancel_user` | 1. 在 `cancel_user` 行点击删除 2. 在确认弹窗点击取消 | 用户列表仍显示 `cancel_user`，该账号仍可使用原密码登录 |
| TC-USER-009 | P0 | 被删除用户不可重新登录 | 管理员已删除账号 `deleted_user` | 1. 打开登录页 2. 输入 `deleted_user` 和原密码 3. 点击登录 | 登录页不跳转，提示“账号或密码错误” |
| TC-USER-010 | P0 | 管理员重置普通用户密码成功 | 管理员已登录，存在普通用户 `reset_user` | 1. 在 `reset_user` 行点击重置密码 2. 输入新密码与确认新密码 3. 提交 | 页面提示重置成功，`reset_user` 可使用新密码重新登录 |
| TC-USER-011 | P0 | 重置普通用户后旧密码失效 | 已完成 TC-USER-010 | 1. 使用 `reset_user` 的旧密码重新登录 | 登录页不跳转，提示“账号或密码错误” |
| TC-USER-012 | P1 | 重置普通用户密码时无需当前密码 | 管理员已登录，存在普通用户 `reset_user_2` | 1. 打开重置密码弹窗 2. 不填写当前密码 3. 输入新密码与确认新密码 4. 提交 | 页面提示重置成功，不要求输入当前密码 |
| TC-USER-013 | P0 | 管理员不能重置内置 admin 密码 | 管理员已登录 | 1. 在 `admin` 账号行打开重置密码入口 | 重置入口不可用或提交被拦截，`admin` 密码不变 |
| TC-USER-014 | P0 | 管理员不能重置其他管理员密码 | 管理员已登录，存在非内置管理员 `mgr_other` | 1. 在 `mgr_other` 行打开重置密码入口 2. 尝试提交新密码 | 重置被拦截，页面提示不可重置管理员密码，账号密码不变 |
| TC-VEH-001 | P0 | 管理员新增车辆成功 | 管理员已登录并进入管理页 | 1. 打开新增车辆弹窗 2. 填写唯一车辆编号、唯一车牌号、品牌型号 3. 提交 | 车辆弹窗关闭，车辆列表新增 1 行，车辆编号、车牌号、品牌型号与输入一致 |
| TC-VEH-002 | P1 | 新增车辆时车辆编号重复 | 已存在车辆编号 `CAR-001` | 1. 填写重复车辆编号 2. 输入其他必填字段 3. 提交 | 车辆列表行数不变，页面提示车辆编号重复 |
| TC-VEH-003 | P1 | 新增车辆时车牌号重复 | 已存在车牌号 | 1. 填写重复车牌号 2. 输入其他必填字段 3. 提交 | 车辆列表行数不变，页面提示车牌号重复 |
| TC-VEH-004 | P1 | 新增车辆时缺少必填字段 | 管理员已登录 | 1. 分别缺失车辆编号、车牌号、品牌型号后提交 | 车辆列表行数不变，缺失字段展示必填提示 |
| TC-VEH-005 | P0 | 管理员删除车辆成功 | 已存在可删除车辆且有无历史记录均可 | 1. 在车辆列表点击删除 2. 确认删除 | 车辆列表删除该车辆行，登记页车辆下拉框不包含该车辆 |
| TC-VEH-006 | P1 | 删除车辆后历史记录保留 | 已删除一辆存在历史记录的车辆 | 1. 进入记录列表 2. 查询已删除车辆历史记录 3. 执行导出 | 页面和导出中仍能看到该车历史记录 |
| TC-VEH-007 | P0 | 普通员工访问车辆管理页 | 普通员工已登录 | 1. 直接访问车辆管理页面或接口 | 页面不可访问或接口返回无权限 |
| TC-VEH-008 | P1 | 删除车辆弹窗点击取消 | 管理员已登录，存在车辆 `CAR-CANCEL` | 1. 在车辆 `CAR-CANCEL` 行点击删除 2. 在确认弹窗点击取消 | 车辆列表仍显示 `CAR-CANCEL`，登记页车辆下拉框仍包含该车辆 |
| TC-REC-001 | P0 | 首次登记时无历史里程 | 已存在一辆从未登记过的车辆 | 1. 登录并进入登记页 2. 选择该车辆 | 起步公里为空，需手动填写 |
| TC-REC-002 | P0 | 有历史记录车辆自动回填起步公里 | 车辆已有上一条有效记录终点公里为 `1000` | 1. 进入登记页 2. 选择该车辆 | 起步公里自动回填为 `1000` |
| TC-REC-003 | P0 | 切换车辆时里程独立加载 | 车辆 A 最近终点公里为 `1000`，车辆 B 最近终点公里为 `500` | 1. 先选择车辆 A 2. 再切换到车辆 B | 表单清空，起步公里显示为 `500`，不受车辆 A 影响 |
| TC-REC-004 | P1 | 切换车辆时清空已填表单内容 | 已进入登记页并填写部分字段 | 1. 填写事由、路线、终点公里等 2. 切换到另一辆车 | 除车辆选择外，其余表单内容被清空 |
| TC-REC-005 | P0 | 普通员工成功提交同日用车登记 | 普通员工已登录，已选中有效车辆 | 1. 填写全部必填项 2. 终点公里大于起步公里 3. 提交 | 记录列表新增 1 条记录，登记人账号为当前普通员工，里程字段与表单一致 |
| TC-REC-006 | P0 | 管理员成功提交用车登记 | 管理员已登录，已选中有效车辆 | 1. 填写全部必填项 2. 提交 | 记录列表新增 1 条记录，登记人账号为当前管理员，里程字段与表单一致 |
| TC-REC-007 | P1 | 终点公里自动计算行车公里数 | 已选中车辆并填写起步公里 | 1. 输入终点公里 2. 观察行车公里数字段 | 行车公里数自动显示为终点公里减起步公里 |
| TC-REC-008 | P0 | 终点公里小于起步公里时提交 | 已填写起步公里 `1000` | 1. 输入终点公里 `900` 2. 点击提交 | 记录列表不新增记录，页面提示终点公里不能小于起步公里 |
| TC-REC-009 | P1 | 跨天时间登记成功 | 已登录并选中车辆 | 1. 日期选择 2026-07-20 2. 出车时间填 `23:00` 3. 还车时间填 `01:00` 4. 填写其他必填项并提交 | 记录列表新增 1 条记录，该记录跨天标识为 `true` 或等价跨天展示 |
| TC-REC-010 | P1 | 必填项缺失时提交登记 | 已进入登记页 | 1. 分别缺少车辆、出车时间、还车时间、事由、路线、起步公里、终点公里、驾驶员签字后提交 | 提交被拦截，并提示对应必填项 |
| TC-REC-011 | P1 | 提交成功后保留当前车辆并刷新起步公里 | 已选中车辆并提交一条终点公里为 `1200` 的记录 | 1. 提交后观察表单状态 | 当前车辆保持选中，起步公里更新为 `1200` |
| TC-REC-012 | P1 | 车辆已删除时旧登记页提交 | 用户在登记页停留，管理员删除了当前车辆 | 1. 回到旧登记页 2. 继续提交当前表单 | 记录列表不新增记录，页面提示车辆不存在或已失效 |
| TC-REC-013 | P1 | 终点公里等于起步公里时提交 | 已选中车辆，起步公里填写 `1000` | 1. 终点公里填写 `1000` 2. 填写其他必填项 3. 点击提交 | 行车公里数显示 `0`，记录列表新增 1 条记录 |
| TC-REC-014 | P1 | 手动修改起步公里后重新计算里程 | 车辆默认起步公里为 `1000` | 1. 将起步公里改为 `980` 2. 终点公里输入 `1050` | 行车公里数显示 `70` |
| TC-REC-015 | P1 | 起步公里输入负数时提交 | 已选中车辆 | 1. 起步公里输入 `-1` 2. 终点公里输入 `100` 3. 填写其他必填项并提交 | 提交被拦截，提示起步公里格式不合法 |
| TC-REC-016 | P1 | 终点公里输入非数字时提交 | 已选中车辆，起步公里填写 `1000` | 1. 终点公里输入 `abc` 2. 填写其他必填项并提交 | 提交被拦截，提示终点公里格式不合法 |
| TC-MGR-001 | P0 | 管理员查看全部记录列表 | 管理员已登录并进入管理页 | 1. 打开记录管理模块 | 可看到全部有效记录 |
| TC-MGR-002 | P1 | 记录列表字段完整展示 | 管理员已进入记录管理模块 | 1. 查看表头和数据列 | 列表包含车辆编号、车牌、登记人账号、日期、出车/还车时间、里程、加油信息、驾驶员、备注等字段 |
| TC-MGR-003 | P0 | 普通员工访问记录管理页 | 普通员工已登录 | 1. 直接访问记录列表页面或接口 | 页面不可访问或接口返回无权限 |
| TC-MGR-004 | P0 | 管理员删除记录成功 | 存在可删除记录 | 1. 点击删除按钮 2. 确认删除 | 记录列表删除该记录行，导出文件中不包含该记录 |
| TC-MGR-005 | P1 | 删除记录后默认起步公里重算 | 某车辆有多条记录，删除最新一条记录 | 1. 删除该车辆最新记录 2. 回到登记页选择该车辆 | 默认起步公里回退为该车辆剩余最新有效记录的终点公里 |
| TC-MGR-006 | P1 | V1 不存在记录编辑入口 | 管理员已进入记录管理模块 | 1. 检查记录列表和详情操作 | 页面不存在编辑按钮或编辑入口 |
| TC-MGR-007 | P1 | 删除记录弹窗点击取消 | 管理员已登录，存在记录 `REC-CANCEL` | 1. 在 `REC-CANCEL` 行点击删除 2. 在确认弹窗点击取消 | 记录列表仍显示 `REC-CANCEL`，导出文件中仍包含该记录 |
| TC-EXP-001 | P0 | 管理员导出全部记录成功 | 管理员已登录且存在记录 | 1. 点击导出 Excel 按钮 | 浏览器下载 `.xlsx` 文件，文件大小大于 0 |
| TC-EXP-002 | P0 | 导出文件字段与列表一致 | 已下载 Excel 文件 | 1. 打开导出文件 2. 对比表头与列表 | Excel 表头名称与管理页记录列表字段名称一致 |
| TC-EXP-003 | P1 | 导出结果按车辆分类展示 | 已下载 Excel 文件且存在多辆车记录 | 1. 打开导出文件 2. 检查数据排序 | Excel 按车辆编号升序排列，同一车辆内按业务日期升序、创建时间升序排列 |
| TC-EXP-004 | P1 | 删除车辆后历史记录仍可导出 | 已删除一辆有历史记录的车辆 | 1. 执行导出 2. 搜索该车辆历史记录 | 导出文件中仍包含该车辆的历史记录 |
| TC-EXP-005 | P1 | 已删除记录不出现在导出结果中 | 已删除一条记录 | 1. 执行导出 2. 搜索已删除记录 | 导出文件中不存在该记录 |
| TC-EXP-006 | P2 | 无记录时执行导出 | 系统内无有效记录 | 1. 点击导出 Excel 按钮 | 浏览器下载 `.xlsx` 文件，文件包含表头且无数据行 |
| TC-EXP-007 | P0 | 导出数据行数与管理页记录数一致 | 管理页存在 N 条有效记录 | 1. 记录管理页有效记录数 2. 点击导出 3. 打开 Excel 文件统计数据行 | Excel 数据行数等于管理页有效记录数 N |
| TC-EXP-008 | P1 | 导出文件格式可打开 | 管理员已登录且存在记录 | 1. 点击导出 2. 使用 Excel 或 WPS 打开下载文件 | 文件可打开，车辆编号、车牌、日期、里程字段可读 |
| TC-AUTH-001 | P0 | 未登录访问登记页 | 当前浏览器无有效登录态 | 1. 直接访问登记页 | 被重定向到登录页或收到未登录响应 |
| TC-AUTH-002 | P0 | 未登录访问管理接口 | 当前无有效登录态 | 1. 调用任一管理员接口 | 接口返回 `401` 或等价未登录响应 |
| TC-AUTH-003 | P0 | 普通员工调用删除记录接口 | 普通员工已登录且存在记录 | 1. 调用删除记录接口 | 接口返回 `403` 或等价无权限响应，记录仍保留 |
| TC-AUTH-004 | P0 | 普通员工调用导出接口 | 普通员工已登录 | 1. 调用导出接口 | 接口返回 `403` 或等价无权限响应，浏览器不下载文件 |
| TC-AUTH-005 | P1 | 越权修改他人密码 | 普通员工已登录且知道其他账号用户名 | 1. 尝试通过接口传入其他用户标识修改密码 | 接口拒绝请求，仅允许修改当前登录人密码 |
