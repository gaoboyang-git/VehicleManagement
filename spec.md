# Web 公务用车使用登记及管理系统 V1 Spec

## 1. 文档信息

- 文档版本：V1.0
- 文档日期：2026-07-20
- 文档状态：可用于设计、开发、测试评审
- 适用范围：企业内部 Web 公务用车使用登记及管理系统

## 2. 产品目标

建设一个企业内部 Web 公务用车使用登记及管理系统，统一完成公车使用登记、车辆档案维护、账号权限管理、密码修改、全量记录查询与 Excel 导出，替代纸质或分散台账，提升管理规范性与可追溯性。

## 3. 目标用户

### 3.1 普通员工

- 登录系统
- 填写并提交公务用车登记
- 修改本人密码
- 无车辆管理、用户管理、全量记录查看、记录删除、导出权限

### 3.2 管理员

- 具备普通员工全部能力
- 新增/删除用户
- 重置普通用户密码
- 新增/删除车辆
- 查看全部用车记录
- 删除任意用车记录
- 导出全部有效记录为 Excel

### 3.3 初始管理员

- 系统预置账号：`admin`
- 系统预置密码：`admin`
- 不可删除
- 可修改本人密码

## 4. 业务范围

### 4.1 V1 范围内

- 账号密码登录
- 本人修改密码
- 公务用车登记
- 公车档案管理
- 用户账号管理
- 全量记录查看
- 记录删除
- Excel 导出

### 4.2 V1 暂不做

- 短信验证码、邮箱验证码、第三方登录、单点登录
- 忘记密码找回
- 管理员重置内置 `admin` 或其他管理员密码
- 首次登录强制改密
- 审批流、派车流、申请流
- 已提交记录编辑、恢复删除、操作审计
- 图片上传、手写签名、电子签名
- 移动端 App、小程序
- 消息通知、邮件通知
- 多组织隔离
- 高级筛选、复杂统计报表、大屏
- GPS、轨迹、地图导航

## 5. 角色权限

| 功能 | 普通员工 | 管理员 |
| --- | --- | --- |
| 登录/退出 | 是 | 是 |
| 修改本人密码 | 是 | 是 |
| 重置普通用户密码 | 否 | 是 |
| 填写并提交用车登记 | 是 | 是 |
| 查看全部记录 | 否 | 是 |
| 删除记录 | 否 | 是 |
| 新增用户 | 否 | 是 |
| 删除用户 | 否 | 是 |
| 新增车辆 | 否 | 是 |
| 删除车辆 | 否 | 是 |
| 导出 Excel | 否 | 是 |

## 6. 核心业务流程

### 6.1 登录流程

1. 用户访问系统首页，进入登录页。
2. 用户输入账号和密码并点击登录。
3. 系统校验账号密码。
4. 校验失败时，页面提示“账号或密码错误”。
5. 校验成功时，进入公务用车登记页。
6. 管理员登录后，登记页额外展示“查看全部”入口。

### 6.2 用车登记流程

1. 用户进入登记页。
2. 选择车辆。
3. 系统读取该车辆最近一条有效记录的终点公里数，作为默认起步公里。
4. 若该车辆无历史记录，则起步公里留空，由用户手动填写。
5. 用户填写日期、出车时间、还车时间、事由、路线、里程、驾驶员等信息。
6. 系统自动计算行车公里数。
7. 用户提交后，系统保存记录。
8. 提交成功后表单清空，但保留当前选中车辆。
9. 系统将当前车辆的默认起步公里更新为本次终点公里。

### 6.3 管理流程

1. 管理员在登记页点击“查看全部”。
2. 进入管理页后可维护用户和车辆。
3. 管理员可查看全部有效记录。
4. 管理员可重置普通用户密码。
5. 管理员可删除记录。
6. 管理员可导出全部有效记录为 Excel。

### 6.4 修改密码流程

1. 已登录用户点击“修改密码”入口。
2. 输入当前密码、新密码、确认新密码。
3. 系统校验当前密码正确性及新密码一致性。
4. 校验通过后更新密码。
5. 修改成功后立即失效当前登录态，用户需重新登录。

## 7. 页面定义

### 7.1 登录页

#### 页面元素

- 账号输入框
- 密码输入框
- 登录按钮

#### 页面规则

- 账号必填
- 密码必填
- 账号不存在或密码错误时统一提示“账号或密码错误”

### 7.2 公务用车登记页

#### 页面元素

- 车辆下拉框，展示格式为“车辆编号 + 车牌号”
- 日期输入框，默认当天
- 出车时间输入框
- 还车时间输入框
- 事由输入框
- 目的地及行车路线输入框
- 起步公里读数输入框
- 终点公里读数输入框
- 行车公里数只读展示框
- 加油费用/数量输入框
- 驾驶员签字输入框
- 备注输入框
- 提交按钮
- 修改密码入口
- 管理员可见“查看全部”按钮

#### 页面交互

- 切换车辆后清空表单已填写内容
- 切换车辆后重新加载该车辆独立里程数据
- 终点公里更新后自动计算行车公里数
- 提交成功后保留当前车辆
- 提交成功后自动刷新默认起步公里

### 7.3 管理页

#### 模块一：用户账号管理

- 用户列表
- 新增用户弹窗
- 重置密码弹窗
- 删除用户按钮

#### 模块二：公车档案管理

- 车辆列表
- 新增车辆弹窗
- 删除车辆按钮

#### 模块三：用车记录管理

- 全量记录表格
- 删除记录按钮
- 导出全部为 Excel 按钮

### 7.4 修改密码页/弹窗

#### 页面元素

- 当前密码输入框
- 新密码输入框
- 确认新密码输入框
- 提交修改按钮
- 取消按钮

#### 页面规则

- 当前密码必填
- 新密码必填
- 确认新密码必填
- 新密码与确认新密码必须一致
- 新密码不能与当前密码相同

## 8. 接口定义

### 8.1 认证接口

#### `POST /api/login`

- 用途：用户登录
- 入参：
  - `username`
  - `password`
- 出参：
  - `userId`
  - `username`
  - `role`
  - `token` 或等价登录态

#### `POST /api/logout`

- 用途：用户退出登录

### 8.2 修改密码接口

#### `POST /api/change-password`

- 用途：修改当前登录用户密码
- 权限：已登录用户
- 入参：
  - `currentPassword`
  - `newPassword`
  - `confirmPassword`
- 出参：
  - `success`
  - `message`

#### `POST /api/users/{id}/reset-password`

- 用途：管理员重置普通用户密码
- 权限：管理员
- 入参：
  - `newPassword`
  - `confirmPassword`
- 约束：
  - 仅可重置普通用户密码
  - 不允许重置内置 `admin` 密码
  - 不允许重置其他管理员密码

### 8.3 用户管理接口

#### `GET /api/users`

- 用途：获取用户列表
- 权限：管理员

#### `POST /api/users`

- 用途：新增用户
- 权限：管理员
- 入参：
  - `username`
  - `password`
  - `role`

#### `DELETE /api/users/{id}`

- 用途：删除用户
- 权限：管理员

### 8.4 车辆管理接口

#### `GET /api/vehicles`

- 用途：获取车辆列表
- 权限：已登录用户

#### `POST /api/vehicles`

- 用途：新增车辆
- 权限：管理员
- 入参：
  - `vehicleCode`
  - `licensePlate`
  - `brandModel`

#### `DELETE /api/vehicles/{id}`

- 用途：删除车辆
- 权限：管理员

#### `GET /api/vehicles/{id}/last-mileage`

- 用途：获取该车辆最近一条有效记录终点公里数
- 权限：已登录用户

### 8.5 用车记录接口

#### `POST /api/records`

- 用途：新增用车记录
- 权限：已登录用户

#### `GET /api/records`

- 用途：获取全部有效记录
- 权限：管理员

#### `DELETE /api/records/{id}`

- 用途：删除记录
- 权限：管理员

### 8.6 导出接口

#### `GET /api/records/export`

- 用途：导出全部有效记录为 Excel
- 权限：管理员

## 9. 数据模型

### 9.1 用户

- `id`
- `username`
- `password`
- `role`：`admin` / `employee`
- `isBuiltinAdmin`
- `createdAt`

### 9.2 车辆

- `id`
- `vehicleCode`
- `licensePlate`
- `brandModel`
- `createdAt`
- `deletedAt` 或等价状态字段

### 9.3 用车记录

- `id`
- `vehicleId`
- `vehicleCodeSnapshot`
- `licensePlateSnapshot`
- `createdByUserId`
- `createdByUsername`
- `date`
- `departTime`
- `returnTime`
- `isCrossDay`
- `reason`
- `route`
- `startMileage`
- `endMileage`
- `travelMileage`
- `fuelInfo`
- `driverName`
- `remark`
- `createdAt`

## 10. 数据与校验规则

### 10.1 账号规则

- 仅支持账号密码登录
- 账号全局唯一
- 初始 `admin` 账号不可删除
- 初始 `admin` 账号可修改本人密码

### 10.2 修改密码规则

- 当前密码必须正确
- 新密码与确认新密码必须一致
- 新密码不能与当前密码相同
- 仅允许修改本人密码
- 修改成功后旧密码失效
- 修改成功后强制重新登录

### 10.3 管理员重置密码规则

- 管理员可重置普通用户密码
- 重置时不需要输入被重置用户的当前密码
- 重置时新密码与确认新密码必须一致
- 重置后该普通用户的旧密码失效
- 重置后该普通用户使用新密码可重新登录
- 不允许重置内置 `admin` 密码
- 不允许重置其他管理员密码

### 10.3 用户管理规则

- 新增用户时账号、密码、角色必填
- 账号重复不可创建
- 管理员可删除普通账号和非内置管理员账号
- 初始 `admin` 账号禁止删除

### 10.4 车辆管理规则

- 车辆编号必填且全局唯一
- 车牌号码必填且全局唯一
- 品牌型号必填
- 删除车辆后不删除历史记录
- 删除车辆后该车辆不再出现在登记页下拉框

### 10.5 用车登记规则

- 车辆必选
- 日期默认当天，可修改
- 出车时间必填
- 还车时间必填
- 事由必填
- 目的地及行车路线必填
- 起步公里必填
- 终点公里必填
- 驾驶员签字必填
- 加油费用/数量可选
- 备注可选

### 10.6 里程规则

- `行车公里数 = 终点公里 - 起步公里`
- 终点公里必须大于或等于起步公里
- 每辆车独立维护里程链路
- 默认起步公里取该车辆最近一条有效记录的终点公里
- 若车辆无历史记录，则起步公里留空，由用户手动填写
- 用户可手动修改起步公里

### 10.7 时间规则

- V1 允许跨天
- 若还车时间早于出车时间，则视为次日还车
- 跨天记录需标记 `isCrossDay = true`
- 记录保留一个业务日期字段，表示出车所属日期

### 10.8 记录管理规则

- 普通员工不可删除记录
- 管理员可删除任意记录
- V1 不支持编辑已提交记录
- 删除记录后，相关车辆默认起步公里按剩余有效记录重新计算

### 10.9 导出规则

- 导出范围为全部有效记录
- Excel 表头与管理页表格字段一致
- 导出结果按车辆分类有序展示
- 已删除车辆的历史记录仍需导出
- 已删除记录不出现在导出文件中

## 11. 异常场景

- 账号不存在或密码错误
- 未登录访问受限页面或接口
- 普通员工访问管理员接口
- 新增用户时账号重复
- 删除初始 `admin` 账号
- 修改密码时当前密码错误
- 修改密码时两次新密码不一致
- 修改密码时新旧密码相同
- 管理员重置普通用户密码时新密码不一致
- 管理员误尝试重置管理员或内置 admin 密码
- 新增车辆时车辆编号重复
- 新增车辆时车牌号重复
- 登记时未选择车辆
- 某车辆无历史记录，起步公里为空
- 终点公里小于起步公里
- 用户录入跨天时间
- 车辆已被删除但用户仍停留在旧登记页尝试提交
- 管理员删除记录后车辆默认起步公里回退
- 导出时无记录
- Excel 导出失败或浏览器下载中断

## 12. 非功能要求

### 12.1 安全性

- 管理接口必须做角色校验
- 密码修改接口必须校验当前登录人身份
- 错误提示不暴露账号是否存在

### 12.2 可用性

- 表单校验提示应明确指出问题字段
- 删除操作需二次确认
- 导出成功后应触发浏览器下载

### 12.3 一致性

- 同一车辆的默认起步公里始终以最近一条有效记录为准
- 车辆删除不影响历史记录查询和导出

## 13. 验收前提

- 系统预置 `admin/admin`
- 至少存在 1 个管理员账号可执行管理功能
- 测试环境支持浏览器文件下载

## 14. 风险点

- 允许手动修改起步公里，存在台账不连续风险
- 允许跨天但只保留一个业务日期字段，后续做统计时可能需要升级时间模型
- 管理员可删除记录但无操作审计，后续追责能力较弱
- 删除记录会影响车辆默认起步公里，需确保按有效记录重算
- 初始账号 `admin/admin` 存在安全风险，上线前建议强制变更
- V1 不做分页时，大量记录可能影响列表与导出性能
- V1 未定义密码复杂度策略，存在弱密码风险

## 15. 默认假设

- 管理员也可以填写并提交用车登记
- V1 不做草稿保存，提交即入库
- 管理页默认展示全量记录
- Excel 导出为单工作表
